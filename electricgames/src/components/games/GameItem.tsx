import IGames from "../../interfaces/IGames";

const GameItem = ({id, title, } : IGames) => {

    return(
        <section>
            <h3>Find a game</h3>
        </section>

    )
}

export default GameItem;